---
interface Props {
  title: string;
  text: string;
  //Icon:
  src: string;
  state: string;
}

const { title, text, src, state, delay } = Astro.props;
---

<div
  class="text-center p-8 h-64 text-white rounded-3xl border border-gray-700 bg-[#070917] bg-opacity-50 hover:bg-opacity-80 ease-in-out transition-all duration-300 cursor-default"
>
  <lord-icon
    src={src}
    trigger="loop"
    state={state}
    delay="600"
    colors="primary:#FF0694,secondary:#FF0694"
    style="width:150px; height:70px"
  >
  </lord-icon>
  <h4 class="text-xl font-extrabold mb-2">{title}</h4>
  <p class="text-sm">{text}</p>
</div>

<script src="https://cdn.lordicon.com/lordicon.js"></script>
